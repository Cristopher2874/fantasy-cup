app:
  host: 127.0.0.1
  port: 8000
  data_dir: data
  runner: mock
  debug: true

football_api:
  provider: api-sports
  league_id:
  season:
  timezone: UTC

features:
  hosted_container_runner: false
  codex_cli_runner: false
